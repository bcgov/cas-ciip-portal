{
  "schema": {
    "type": "array",
    "items": {
      "$ref": "#/definitions/fuel"
    },
    "definitions": {
      "fuel": {
        "type": "object",
        "properties": {
          "fuelRowId": {
            "title": "Fuel Name",
            "type": "number"
          },
          "quantity": {
            "title": "Quantity",
            "type": "number"
          },
          "fuelUnits": {
            "title": "Units",
            "type": "string"
          }
        },
        "required": ["fuelRowId", "quantity", "fuelUnits"]
      }
    }
  },
  "uiSchema": {
    "ui:add-text": "Add a fuel",
    "ui:remove-text": "Remove fuel",
    "items": {
      "ui:field": "fuel",
      "ui:order": ["fuelRowId", "quantity", "fuelUnits"],
      "fuelRowId": {
        "ui:widget": "SearchWidget",
        "ui:field": "fuelRowId",
        "ui:col-md": 6
      },
      "fuelUnits": {
        "ui:readonly": true,
        "ui:col-md": 2
      },
      "quantity": {
        "ui:col-md": 4
      }
    }
  }
}
