{
  "schema": {
    "type": "array",
    "items": {
      "$ref": "#/definitions/fuel"
    },
    "definitions": {
      "fuel": {
        "type": "object",
        "properties": {
          "fuelType": {
            "title": "Fuel Type",
            "type": "string"
          },
          "quantity": {
            "title": "Quantity",
            "type": "number"
          },
          "fuelUnits": {
            "title": "Units",
            "type": "string"
          },
          "methodology": {
            "title": "Methodology",
            "type": "string",
            "enum": ["wci 1.0", "wci 2.0", "wci 3.0"],
            "enumNames": ["WCI 1.0", "WCI 2.0", "WCI 3.0"]
          }
        },
        "required": ["Fuel Type", "Quantity", "Units", "Methodology"]
      }
    }
  },
  "uiSchema": {
    "ui:add-text": "Add a fuel",
    "ui:remove-text": "Remove fuel",
    "items": {
      "ui:field": "fuel"
    }
  }
}
