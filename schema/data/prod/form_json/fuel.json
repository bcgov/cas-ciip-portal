{
  "schema": {
    "type": "array",
    "items": {
      "$ref": "#/definitions/fuel"
    },
    "definitions": {
      "fuel": {
        "type": "object",
        "properties": {
          "fuelRowId": {
            "title": "Fuel Name",
            "type": "number"
          },
          "quantity": {
            "title": "Quantity",
            "type": "number"
          },
          "fuelUnits": {
            "title": "Units",
            "type": "string"
          },
          "methodology": {
            "title": "Methodology",
            "type": "string",
            "enum": ["wci 1.0", "wci 2.0", "wci 3.0"],
            "enumNames": ["WCI 1.0", "WCI 2.0", "WCI 3.0"]
          }
        },
        "required": ["fuelRowId", "quantity", "fuelUnits", "methodology"]
      }
    }
  },
  "uiSchema": {
    "ui:add-text": "Add a fuel",
    "ui:remove-text": "Remove fuel",
    "items": {
      "ui:field": "fuel",
      "ui:order": ["fuelRowId", "quantity", "fuelUnits", "methodology"],
      "fuelRowId": {
        "ui:widget": "SearchWidget",
        "ui:field": "fuelRowId",
        "ui:col-md": 6
      },
      "fuelUnits": {
        "ui:readonly": true,
        "ui:col-md": 2
      },
      "quantity": {
        "ui:col-md": 4
      }
    }
  }
}
