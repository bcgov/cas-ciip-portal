{
  "schema": {
    "type": "array",
    "items": {
      "$ref": "#/definitions/product"
    },
    "definitions": {
      "product": {
        "type": "object",
        "properties": {
          "productRowId": {
            "title": "Product or Service",
            "type": "integer"
          },
          "quantity": {
            "title": "Annual Production Amount",
            "type": "number"
          },
          "productUnits": {
            "title": "Units",
            "type": "string"
          },
          "productEmissions": {
            "title": "Emission allocated to product",
            "type": "number",
            "exclusiveMinimum": 0
          },
          "importedElectricityAllocationFactor": {
            "title": "Imported electricity allocation factor (%)",
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "importedHeatAllocationFactor": {
            "title": "Imported heat allocation factor (%)",
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "paymentAllocationFactor": {
            "title": "Payment allocation factor (%)",
            "type": "integer",
            "minimum": 0,
            "maximum": 100
          },
          "additionalData": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "required": [
          "productRowId",
          "quantity",
          "productUnits",
          "productEmissions",
          "paymentAllocationFactor"
        ]
      }
    }
  },
  "uiSchema": {
    "ui:add-text": "Add a Product",
    "ui:remove-text": "Remove Product",
    "items": {
      "ui:field": "production",
      "productRowId": {
        "ui:col-md": 8,
        "ui:widget": "SearchWidget"
      },
      "productEmissions": {
        "ui:col-md": 4,
        "ui:help": "Report emission intensity associated to CIIP product, in tCO2e"
      }
    }
  }
}
